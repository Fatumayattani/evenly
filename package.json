{
  "name": "evenly",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @evenly/web",
    "build": "npm run build --workspace @evenly/core && npm run build --workspace @evenly/web",
    "typecheck": "npm run typecheck --workspace @evenly/core && npm run typecheck --workspace @evenly/web",
    "test": "npm run test --workspace @evenly/core",
    "clean": "rm -rf node_modules apps/web/.next packages/core/dist"
  },
  "engines": {
    "node": ">=20"
  }
}